# Optional: only install these if you set ENABLE_AI_ADVICE=true and are
# deploying somewhere with enough RAM/CPU (GPT-2 + sentence-transformers +
# FAISS are too heavy for most free hosting tiers).
#
#   pip install -r requirements.txt -r requirements-ai.txt

langchain-huggingface==0.0.3
langchain-community==0.2.16
langchain-core==0.2.38
sentence-transformers==3.0.1
faiss-cpu==1.8.0
transformers==4.44.2
torch==2.4.1
